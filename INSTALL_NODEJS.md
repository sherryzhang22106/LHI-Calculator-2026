# 📦 Node.js 安装指南

## ⚠️ 安装说明

由于系统权限限制，无法自动安装Node.js。请按照以下步骤手动安装。

---

## 🍎 macOS 安装方法

### **方法1：官方安装包（推荐，最简单）**

1. **访问Node.js官网**：
   - 打开浏览器访问：https://nodejs.org/
   
2. **下载LTS版本**：
   - 点击绿色的 "LTS" 按钮下载（推荐使用长期支持版本）
   - 文件名类似：`node-v20.x.x.pkg`

3. **运行安装包**：
   - 双击下载的 `.pkg` 文件
   - 按照安装向导完成安装
   - 过程中可能需要输入管理员密码

4. **验证安装**：
   ```bash
   node --version   # 应显示 v20.x.x
   npm --version    # 应显示 10.x.x
   ```

✅ **安装完成后，返回终端运行项目启动命令！**

---

### **方法2：使用Homebrew（推荐，适合开发者）**

如果你已经安装了Homebrew，或想要使用包管理器：

1. **安装Homebrew**（如果未安装）：
   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```
   
   > 注意：需要管理员权限，安装过程可能需要5-10分钟

2. **使用Homebrew安装Node.js**：
   ```bash
   brew install node
   ```

3. **验证安装**：
   ```bash
   node --version
   npm --version
   ```

---

### **方法3：使用nvm（推荐，可管理多版本）**

适合需要在多个Node.js版本之间切换的开发者：

1. **安装nvm**：
   ```bash
   curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
   ```

2. **重启终端或运行**：
   ```bash
   source ~/.zshrc  # 或 source ~/.bash_profile
   ```

3. **安装Node.js**：
   ```bash
   nvm install --lts
   nvm use --lts
   ```

4. **验证安装**：
   ```bash
   node --version
   npm --version
   ```

---

## 🪟 Windows 安装方法

1. 访问：https://nodejs.org/
2. 下载Windows安装包（.msi文件）
3. 运行安装包，按向导完成安装
4. 重启命令提示符或PowerShell
5. 验证：`node --version`

---

## 🐧 Linux 安装方法

### Ubuntu/Debian
```bash
# 使用官方仓库
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证
node --version
npm --version
```

### CentOS/RHEL
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

---

## ✅ 安装完成后的步骤

1. **重启终端**（重要！确保PATH更新）

2. **验证安装**：
   ```bash
   node --version   # 应该显示版本号
   npm --version    # 应该显示版本号
   ```

3. **回到项目目录**：
   ```bash
   cd /Users/a1/Downloads/001/lhi-calculator
   ```

4. **安装项目依赖**：
   ```bash
   # 安装前端依赖
   npm install
   
   # 安装后端依赖
   cd server
   npm install
   cd ..
   ```

5. **初始化数据库**：
   ```bash
   cd server
   npx prisma generate
   npx prisma migrate dev --name init
   npm run seed
   cd ..
   ```

6. **启动项目**：
   ```bash
   # 方式1：一键启动
   ./start.sh
   
   # 方式2：分别启动
   # 终端1
   cd server && npm run dev
   
   # 终端2（新开终端）
   npm run dev
   ```

---

## 🔍 常见问题

### ❓ 安装后仍然提示"command not found"

**解决方案**：
1. 关闭当前终端，重新打开
2. 检查PATH环境变量：
   ```bash
   echo $PATH
   ```
3. 确认Node.js安装路径：
   ```bash
   which node
   ```

### ❓ npm安装速度慢

**解决方案**：使用国内镜像
```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com/

# 验证
npm config get registry
```

### ❓ 权限错误：EACCES

**解决方案**：
```bash
# 修复npm权限
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) /usr/local/lib/node_modules
```

### ❓ 需要特定版本的Node.js

项目要求：**Node.js 18.x 或更高版本**

检查版本：
```bash
node --version
```

如果版本过低，重新安装最新的LTS版本。

---

## 📞 获取帮助

如果遇到安装问题：

1. **查看官方文档**：https://nodejs.org/en/download/
2. **搜索错误信息**：复制错误信息到Google搜索
3. **检查系统要求**：
   - macOS 10.15 或更高
   - 至少2GB可用磁盘空间
   - 管理员权限

---

## 🎯 快速验证清单

安装完成后，请确认以下命令都能正常运行：

- [ ] `node --version` → 显示 v18.x.x 或更高
- [ ] `npm --version` → 显示 9.x.x 或更高
- [ ] `npx --version` → 显示版本号
- [ ] `cd /Users/a1/Downloads/001/lhi-calculator && npm install` → 安装成功

全部✅后，即可开始运行项目！

---

## 🚀 推荐安装路径

**最简单的方法**：
1. 访问 https://nodejs.org/
2. 点击绿色的 "LTS" 按钮下载
3. 双击安装包，一路下一步
4. 重启终端
5. 运行 `node --version` 验证

**总耗时**：约5分钟

---

**提示**：安装完Node.js后，记得回到项目目录运行 `./start.sh` 启动项目！
